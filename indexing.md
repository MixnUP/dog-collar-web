PersonA Query Description (short paragraph)

This query retrieves up to 100 documents from the PersonA collection, ordered by the timestamp field in descending order, allowing the most recent proximity logs to appear first for quick activity review.

PersonB Query Description (short paragraph)

This query pulls the latest 100 documents from the PersonB collection, sorted by timestamp in descending order, making it easy to monitor the newest recorded interactions at the top.